# ============================================================
# RAG Pipeline — Trabalho 2 — Aprendizado de Máquina
# Prof. Mozart Hasse
#
# ATENÇÃO: instale o PyTorch ANTES deste arquivo com a URL do seu CUDA (veja nvidia-smi):
#   CUDA 12.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   CUDA 12.4: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Depois:
#   pip install -r requirements.txt
# ============================================================

# --- Extração de PDF ---
# Versão 1: pdfplumber + camelot-py[cv] (requeria Ghostscript no sistema)
# Versão atual: Docling — extração de texto e tabelas via modelos ML, sem Ghostscript
docling>=2.0.0           # extração de PDF com layout ML (texto + tabelas em markdown)

# --- Chunking ---
langchain-text-splitters>=0.3.0

# --- Embeddings ---
sentence-transformers>=3.3.0
transformers>=4.47.0
huggingface-hub>=0.27.0

# --- Vector Store ---
chromadb>=0.6.0

# --- Numpy ---
numpy>=2.0.0             # numpy 2.x é o padrão para Python 3.13

# --- API ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# --- Interface ---
streamlit>=1.40.0

# --- Validação ---
pydantic>=2.0.0

# --- Testes ---
pytest>=8.3.0
pytest-cov>=6.0.0

# --- Utilitários ---
python-dotenv>=1.0.0
tqdm>=4.67.0
requests>=2.32.0
